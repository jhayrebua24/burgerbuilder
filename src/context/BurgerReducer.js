import {
  ADD_BUILD,
  DELETE_BUILD,
} from './actions';

const reducer = (state = {}, action) => {
  const { payload, type } = action;
  switch (type) {
  default:
    return state;
  }
}
export default reducer;
